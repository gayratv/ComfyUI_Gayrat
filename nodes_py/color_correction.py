"""
Особенности улучшенной версии:
Включенные параметры:

Из оригинала:

✅ gamma, contrast, exposure, offset
✅ hue, saturation, value
✅ clamp
✅ поддержка масок


Дополнительные улучшения:

✅ brightness - более интуитивный параметр яркости
✅ highlights/shadows - раздельная обработка светлых и темных областей
✅ temperature/tint - цветовая коррекция для стилизации



Технические улучшения:

🚀 Полностью на PyTorch (быстрая GPU обработка)
🔧 Нет зависимости от OpenCV или mtb utils
📊 Более точные алгоритмы HSV преобразования на тензорах
🎯 Оптимизированная последовательность применения эффектов
🎭 Улучшенная поддержка масок с автоматическим расширением размерности

Преимущества над оригиналом:

Больше творческого контроля
Профессиональные инструменты цветокоррекции (temperature/tint, highlights/shadows)
Полностью автономный код без внешних зависимостей
Сохранена совместимость с HDR workflows через параметр clamp
Более эффективная обработка на GPU

Эта версия дает вам мощный инструмент цветокоррекции, сравнимый с профессиональными редакторами, но полностью интегрированный в ComfyUI workflow.
"""

"""
MTB_ColorCorrect - Продвинутая нода цветокоррекции для ComfyUI

ОПИСАНИЕ ПАРАМЕТРОВ:

1. ЯРКОСТЬ (Brightness) [0.0-2.0, по умолчанию: 1.0]
   - Общая яркость изображения
   - 1.0 = без изменений
   - 0.5 = в два раза темнее
   - 2.0 = в два раза ярче
   - Рекомендуется: 0.8-1.2 для естественной коррекции
   - Default: 1.0

2. КОНТРАСТ (Contrast) [0.0-5.0, по умолчанию: 1.0]
   - Разница между светлыми и темными областями
   - 1.0 = без изменений
   - <1.0 = мягкое изображение
   - >1.0 = резкое изображение
   - Рекомендуется: 0.8-1.3 для сохранения деталей
   - Default: 1.0

3. ГАММА (Gamma) [0.1-5.0, по умолчанию: 1.0]
   - Нелинейная коррекция средних тонов
   - <1.0 = осветление средних тонов
   - >1.0 = затемнение средних тонов
   - Рекомендуется: 0.8-1.2 для коррекции экспозиции
   - Default: 1.0

4. ЭКСПОЗИЦИЯ (Exposure) [-5.0 до 5.0, по умолчанию: 0.0]
   - Имитация изменения выдержки камеры
   - +1.0 = в два раза больше света
   - -1.0 = в два раза меньше света
   - Рекомендуется: ±0.5 для реалистичной коррекции
   - Default: 0.0

5. СМЕЩЕНИЕ (Offset) [-1.0 до 1.0, по умолчанию: 0.0]
   - Сдвиг черной точки (аддитивный)
   - Полезно для коррекции недоэкспонированных изображений
   - Рекомендуется: ±0.1 для тонкой настройки
   - Default: 0.0

6. ОТТЕНОК (Hue) [-0.5 до 0.5, по умолчанию: 0.0]
   - Сдвиг по цветовому кругу
   - ±0.5 = поворот на 180 градусов
   - Рекомендуется: ±0.05 для тонкой цветокоррекции
   - Default: 0.0

7. НАСЫЩЕННОСТЬ (Saturation) [0.0-5.0, по умолчанию: 1.0]
   - Интенсивность цветов
   - 0.0 = черно-белое изображение
   - 1.0 = оригинальные цвета
   - >1.0 = более насыщенные цвета
   - Рекомендуется: 0.8-1.3 для естественного вида
   - Default: 1.0

8. ЯРКОСТЬ HSV (Value) [0.0-5.0, по умолчанию: 1.0]
   - Яркость в цветовой модели HSV
   - Влияет на светлоту без изменения оттенка
   - Рекомендуется: 0.8-1.2
   - Default: 1.0

9. СВЕТА (Highlights) [-1.0 до 1.0, по умолчанию: 0.0]
   - Коррекция только светлых областей
   - <0 = затемнение светов (восстановление деталей)
   - >0 = осветление светов
   - Рекомендуется: -0.3 до 0.3
   - Default: 0.0

10. ТЕНИ (Shadows) [-1.0 до 1.0, по умолчанию: 0.0]
    - Коррекция только темных областей
    - >0 = осветление теней (проявление деталей)
    - <0 = затемнение теней
    - Рекомендуется: -0.3 до 0.3
    - Default: 0.0

11. ТЕМПЕРАТУРА (Temperature) [-100 до 100, по умолчанию: 0]
    - Баланс теплых/холодных тонов
    - >0 = теплее (желтый оттенок)
    - <0 = холоднее (синий оттенок)
    - Рекомендуется: ±30 для естественной коррекции
    - Default: 0.0

12. ОТТЕНОК ЦВЕТА (Tint) [-100 до 100, по умолчанию: 0]
    - Баланс зеленый/пурпурный
    - >0 = пурпурный оттенок
    - <0 = зеленый оттенок
    - Рекомендуется: ±20 для коррекции оттенков кожи
    - Default: 0.0

13. ОГРАНИЧЕНИЕ (Clamp) [True/False, по умолчанию: True]
    - True = ограничить значения в диапазоне [0,1]
    - False = сохранить HDR значения для спецэффектов
    - Отключайте только для bloom, glow и других HDR эффектов
    - Default: True

14. МАСКА (Mask) [опционально]
    - Определяет области применения эффекта
    - Белый = применить коррекцию
    - Черный = оставить без изменений
    - Серый = частичное применение
    - Default: None (применить ко всему изображению)
"""

import torch
import numpy as np
from PIL import Image
from typing import Optional


class MTB_ColorCorrect:
    """Продвинутая нода цветокоррекции с комплексными настройками

    Возможности:
    - Базовые настройки: яркость, контраст, гамма, экспозиция, смещение
    - Продвинутые настройки: света/тени, температура/оттенок
    - HSV настройки: оттенок, насыщенность, яркость
    - Опциональное ограничение значений для HDR рабочих процессов
    - Поддержка масок для выборочного применения
    - Ускорение на GPU

    Рекомендации по использованию:
    1. Начните с базовых параметров (яркость, контраст)
    2. Используйте гамму для коррекции средних тонов
    3. Настройте света/тени для восстановления деталей
    4. Примените температуру/оттенок для стилизации
    5. Отключите clamp только для спецэффектов
    """

    @classmethod
    def INPUT_TYPES(cls):
        return {
            "required": {
                # ВХОДНОЕ ИЗОБРАЖЕНИЕ - изображение для цветокоррекции
                "image": ("IMAGE",),

                # ЯРКОСТЬ - общая яркость изображения
                # 1.0 = без изменений, 0.5 = темнее вдвое, 2.0 = ярче вдвое
                # Рекомендуется: 0.8-1.2 для тонкой настройки
                "brightness": ("FLOAT", {
                    "default": 1.0,
                    "min": 0.0,
                    "max": 2.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Яркость: 1.0=без изменений, <1=темнее, >1=ярче. Рекомендуется 0.8-1.2"
                }),

                # КОНТРАСТ - разница между светлыми и темными областями
                # 1.0 = без изменений, 0 = плоское изображение, 2.0 = усиленный контраст
                # Рекомендуется: 0.8-1.3 для естественного вида
                "contrast": ("FLOAT", {
                    "default": 1.0,
                    "min": 0.0,
                    "max": 5.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Контраст: 1.0=без изменений, <1=мягче, >1=резче. Рекомендуется 0.8-1.3"
                }),

                # ГАММА - нелинейная коррекция средних тонов
                # 1.0 = без изменений, <1 = светлее средние тона, >1 = темнее средние тона
                # Рекомендуется: 0.8-1.2 для коррекции экспозиции без потери деталей
                "gamma": ("FLOAT", {
                    "default": 1.0,
                    "min": 0.1,
                    "max": 5.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Гамма: коррекция средних тонов. <1=светлее, >1=темнее. Рекомендуется 0.8-1.2"
                }),

                # ЭКСПОЗИЦИЯ - имитация изменения выдержки камеры
                # 0 = без изменений, +1 = в 2 раза ярче, -1 = в 2 раза темнее
                # Рекомендуется: -0.5 до +0.5 для реалистичной коррекции
                "exposure": ("FLOAT", {
                    "default": 0.0,
                    "min": -5.0,
                    "max": 5.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Экспозиция: имитация выдержки. ±1=удвоение/уполовинивание света. Рекомендуется ±0.5"
                }),

                # СМЕЩЕНИЕ - добавление/вычитание яркости ко всему изображению
                # 0 = без изменений, полезно для коррекции черной точки
                # Рекомендуется: -0.1 до +0.1 для тонкой настройки
                "offset": ("FLOAT", {
                    "default": 0.0,
                    "min": -1.0,
                    "max": 1.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Смещение: сдвиг черной точки. Рекомендуется ±0.1 для тонкой настройки"
                }),

                # ОТТЕНОК - сдвиг по цветовому кругу
                # 0 = без изменений, ±0.5 = полный оборот (180°)
                # Рекомендуется: ±0.05 для тонкой цветокоррекции
                "hue": ("FLOAT", {
                    "default": 0.0,
                    "min": -0.5,
                    "max": 0.5,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Оттенок: поворот цветового круга. ±0.5=180°. Рекомендуется ±0.05"
                }),

                # НАСЫЩЕННОСТЬ - интенсивность цветов
                # 1.0 = без изменений, 0 = ч/б, 2.0 = удвоенная насыщенность
                # Рекомендуется: 0.8-1.3 для естественных цветов
                "saturation": ("FLOAT", {
                    "default": 1.0,
                    "min": 0.0,
                    "max": 5.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Насыщенность: интенсивность цветов. 0=ч/б, 1=норма. Рекомендуется 0.8-1.3"
                }),

                # ЯРКОСТЬ HSV - яркость в цветовой модели HSV
                # 1.0 = без изменений, влияет на общую светлоту без изменения оттенка
                # Рекомендуется: 0.8-1.2 для сохранения цветового баланса
                "value": ("FLOAT", {
                    "default": 1.0,
                    "min": 0.0,
                    "max": 5.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Яркость HSV: светлота без изменения цвета. Рекомендуется 0.8-1.2"
                }),

                # СВЕТА - коррекция только светлых областей
                # 0 = без изменений, +1 = осветление светов, -1 = затемнение светов
                # Рекомендуется: ±0.3 для восстановления деталей в светах
                "highlights": ("FLOAT", {
                    "default": 0.0,
                    "min": -1.0,
                    "max": 1.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Света: коррекция ярких областей. -=восстановление деталей. Рекомендуется ±0.3"
                }),

                # ТЕНИ - коррекция только темных областей
                # 0 = без изменений, +1 = осветление теней, -1 = затемнение теней
                # Рекомендуется: ±0.3 для проработки деталей в тенях
                "shadows": ("FLOAT", {
                    "default": 0.0,
                    "min": -1.0,
                    "max": 1.0,
                    "step": 0.01,
                    "display": "slider",
                    "tooltip": "Тени: коррекция темных областей. +=осветление теней. Рекомендуется ±0.3"
                }),

                # ТЕМПЕРАТУРА - баланс между теплыми и холодными тонами
                # 0 = без изменений, + = теплее (желтый), - = холоднее (синий)
                # Рекомендуется: ±30 для естественной коррекции
                "temperature": ("FLOAT", {
                    "default": 0.0,
                    "min": -100.0,
                    "max": 100.0,
                    "step": 1.0,
                    "display": "slider",
                    "tooltip": "Температура: +=теплее (желтый), -=холоднее (синий). Рекомендуется ±30"
                }),

                # ОТТЕНОК ЦВЕТА - баланс между зеленым и пурпурным
                # 0 = без изменений, + = пурпурный оттенок, - = зеленый оттенок
                # Рекомендуется: ±20 для коррекции оттенка кожи
                "tint": ("FLOAT", {
                    "default": 0.0,
                    "min": -100.0,
                    "max": 100.0,
                    "step": 1.0,
                    "display": "slider",
                    "tooltip": "Оттенок: +=пурпурный, -=зеленый. Для коррекции кожи рекомендуется ±20"
                }),

                # ОГРАНИЧЕНИЕ - ограничивать ли значения в диапазоне [0,1]
                # True = стандартный режим, False = HDR режим для спецэффектов
                "clamp": ("BOOLEAN", {
                    "default": True,
                    "tooltip": "Ограничение значений [0,1]. Отключите для HDR и спецэффектов (bloom/glow)"
                }),
            },
            "optional": {
                # МАСКА - опциональная маска для выборочного применения эффектов
                # Белые области = применение эффекта, черные = без изменений
                "mask": ("MASK",),
            }
        }

    RETURN_TYPES = ("IMAGE",)
    FUNCTION = "correct"
    CATEGORY = "Gayrat/image processing"

    @staticmethod
    def tensor2pil(tensor):
        """Конвертация тензора в PIL изображения

        Поддерживает:
        - Одиночные изображения (H,W,C)
        - Батчи изображений (B,H,W,C)
        """
        if len(tensor.shape) == 4:
            # Batch of images
            return [Image.fromarray(np.clip(255. * t.cpu().numpy(), 0, 255).astype(np.uint8)) for t in tensor]
        else:
            # Single image
            return Image.fromarray(np.clip(255. * tensor.cpu().numpy(), 0, 255).astype(np.uint8))

    @staticmethod
    def pil2tensor(pil_images):
        """Конвертация PIL изображений обратно в тензор

        Автоматически нормализует значения в диапазон [0,1]
        и собирает батч при необходимости
        """
        if isinstance(pil_images, list):
            # Batch of images
            tensors = [torch.from_numpy(np.array(img).astype(np.float32) / 255.0) for img in pil_images]
            return torch.stack(tensors)
        else:
            # Single image
            return torch.from_numpy(np.array(pil_images).astype(np.float32) / 255.0).unsqueeze(0)

    @staticmethod
    def rgb_to_hsv_torch(image: torch.Tensor):
        """Конвертация RGB в HSV используя операции PyTorch

        HSV (Hue, Saturation, Value) позволяет независимо настраивать:
        - Оттенок (Hue): положение на цветовом круге [0,1]
        - Насыщенность (Saturation): интенсивность цвета [0,1]
        - Яркость (Value): светлота цвета [0,1]
        """
        r, g, b = image.unbind(-1)
        max_rgb, argmax_rgb = image.max(-1)
        min_rgb, _ = image.min(-1)
        diff = max_rgb - min_rgb

        h = torch.zeros_like(max_rgb)
        s = diff / (max_rgb + 1e-7)
        v = max_rgb

        # Calculate hue
        h[argmax_rgb == 0] = (g - b)[argmax_rgb == 0] / (diff + 1e-7)[argmax_rgb == 0]
        h[argmax_rgb == 1] = 2.0 + (b - r)[argmax_rgb == 1] / (diff + 1e-7)[argmax_rgb == 1]
        h[argmax_rgb == 2] = 4.0 + (r - g)[argmax_rgb == 2] / (diff + 1e-7)[argmax_rgb == 2]

        h = (h / 6.0) % 1.0
        h = h.unsqueeze(-1)
        s = s.unsqueeze(-1)
        v = v.unsqueeze(-1)

        return torch.cat((h, s, v), dim=-1)

    @staticmethod
    def hsv_to_rgb_torch(hsv: torch.Tensor):
        """Конвертация HSV обратно в RGB

        Использует стандартный алгоритм преобразования HSV->RGB
        с обработкой 6 секторов цветового круга
        """
        h, s, v = hsv.unbind(-1)
        h = h * 6.0
        i = torch.floor(h)
        f = h - i

        p = v * (1.0 - s)
        q = v * (1.0 - s * f)
        t = v * (1.0 - s * (1.0 - f))

        i = i.long() % 6

        # Создаем маски для каждого сектора цветового круга
        mask = torch.stack((i == 0, i == 1, i == 2, i == 3, i == 4, i == 5), -1)

        # Вычисляем RGB значения для каждого сектора
        # Сектор 0: R=v, G=t, B=p
        # Сектор 1: R=q, G=v, B=p
        # Сектор 2: R=p, G=v, B=t
        # Сектор 3: R=p, G=q, B=v
        # Сектор 4: R=t, G=p, B=v
        # Сектор 5: R=v, G=p, B=q

        # Красный канал
        r = v * mask[..., 0] + q * mask[..., 1] + p * mask[..., 2] + \
            p * mask[..., 3] + t * mask[..., 4] + v * mask[..., 5]

        # Зеленый канал
        g = t * mask[..., 0] + v * mask[..., 1] + v * mask[..., 2] + \
            q * mask[..., 3] + p * mask[..., 4] + p * mask[..., 5]

        # Синий канал
        b = p * mask[..., 0] + p * mask[..., 1] + t * mask[..., 2] + \
            v * mask[..., 3] + v * mask[..., 4] + q * mask[..., 5]

        return torch.stack((r, g, b), dim=-1)

    @staticmethod
    def adjust_highlights_shadows(image: torch.Tensor, highlights: float, shadows: float):
        """Раздельная настройка светлых и темных областей

        Использует маски на основе яркости для выборочного воздействия:
        - Света: воздействие пропорционально квадрату яркости
        - Тени: воздействие пропорционально квадрату (1-яркость)
        """
        # Calculate luminance using Rec. 709 weights
        luminance = 0.2126 * image[..., 0] + 0.7152 * image[..., 1] + 0.0722 * image[..., 2]

        # Create smooth masks for highlights and shadows
        highlight_mask = torch.pow(luminance, 2.0).unsqueeze(-1)
        shadow_mask = torch.pow(1.0 - luminance, 2.0).unsqueeze(-1)

        # Apply adjustments
        if highlights != 0.0:
            image = image + highlights * highlight_mask * (1.0 - image)

        if shadows != 0.0:
            image = image + shadows * shadow_mask * image

        return image

    @staticmethod
    def adjust_temperature_tint(image: torch.Tensor, temperature: float, tint: float):
        """Настройка цветовой температуры и оттенка

        Температура: сдвиг по оси синий-желтый (холодный-теплый)
        Оттенок: сдвиг по оси зеленый-пурпурный
        Имитирует настройки баланса белого в камере
        """
        # Temperature adjustment matrix (blue-yellow axis)
        temp_scale = temperature / 100.0

        # Tint adjustment matrix (green-magenta axis)
        tint_scale = tint / 100.0

        # Apply temperature (affects red and blue channels)
        if temperature != 0:
            if temperature > 0:
                # Warmer (more yellow/red)
                image[..., 0] = image[..., 0] * (1.0 + temp_scale * 0.1)  # Red
                image[..., 2] = image[..., 2] * (1.0 - temp_scale * 0.1)  # Blue
            else:
                # Cooler (more blue)
                image[..., 0] = image[..., 0] * (1.0 + temp_scale * 0.1)  # Red
                image[..., 2] = image[..., 2] * (1.0 - temp_scale * 0.1)  # Blue

        # Apply tint (affects green channel)
        if tint != 0:
            image[..., 1] = image[..., 1] * (1.0 - tint_scale * 0.1)  # Green

        return image

    def correct(
            self,
            image: torch.Tensor,
            brightness: float,
            contrast: float,
            gamma: float,
            exposure: float,
            offset: float,
            hue: float,
            saturation: float,
            value: float,
            highlights: float,
            shadows: float,
            temperature: float,
            tint: float,
            clamp: bool,
            mask: Optional[torch.Tensor] = None,
    ):
        """Применение комплексной цветокоррекции

        Последовательность применения эффектов оптимизирована для:
        1. Минимизации потери информации
        2. Естественного вида результата
        3. Предсказуемого поведения параметров
        """

        # Подготовка маски при необходимости
        if mask is not None:
            # Ensure mask has same batch size
            if mask.shape[0] != image.shape[0]:
                if mask.shape[0] == 1:
                    mask = mask.expand(image.shape[0], -1, -1)
                else:
                    raise ValueError("Mask batch size must match image batch size or be 1")

            # Expand mask to have same channels as image
            if mask.dim() == 3:
                mask = mask.unsqueeze(-1)
            mask = mask.expand(-1, -1, -1, image.shape[-1])

        # Применяем эффекты в оптимальном порядке для лучших результатов
        adjusted = image.clone()

        # 1. ЭКСПОЗИЦИЯ - первой, так как это базовая коррекция света
        # Имитирует изменение выдержки камеры (мультипликативная)
        if exposure != 0.0:
            adjusted = adjusted * (2.0 ** exposure)

        # 2. ЯРКОСТЬ - общее масштабирование интенсивности
        # Применяется после экспозиции для дополнительной настройки
        if brightness != 1.0:
            adjusted = adjusted * brightness

        # 3. ГАММА - нелинейная коррекция средних тонов
        # Не затрагивает черную и белую точки
        if gamma != 1.0:
            adjusted = torch.pow(adjusted, 1.0 / gamma)

        # 4. КОНТРАСТ - усиление разницы от среднего значения
        # Использует яркость для сохранения цветового баланса
        if contrast != 1.0:
            # Расчет яркости по Rec. 709
            luminance = 0.2126 * adjusted[..., 0] + 0.7152 * adjusted[..., 1] + 0.0722 * adjusted[..., 2]
            luminance_mean = torch.mean(luminance)

            # Применение контраста относительно средней яркости
            adjusted = (adjusted - luminance_mean) * contrast + luminance_mean

        # 5. СМЕЩЕНИЕ - аддитивная коррекция черной точки
        # Применяется после мультипликативных операций
        if offset != 0.0:
            adjusted = adjusted + offset

        # 6. СВЕТА И ТЕНИ - локальная коррекция по яркости
        # Позволяет восстановить детали без влияния на средние тона
        if highlights != 0.0 or shadows != 0.0:
            adjusted = MTB_ColorCorrect.adjust_highlights_shadows(adjusted, highlights, shadows)

        # 7. ТЕМПЕРАТУРА И ОТТЕНОК - цветовой баланс
        # Применяется после яркостных коррекций
        if temperature != 0.0 or tint != 0.0:
            adjusted = MTB_ColorCorrect.adjust_temperature_tint(adjusted, temperature, tint)

        # 8. HSV КОРРЕКЦИИ - финальная цветовая настройка
        # Применяется последней для точного контроля цвета
        if hue != 0.0 or saturation != 1.0 or value != 1.0:
            # Конвертация в HSV для независимой настройки цветовых компонентов
            hsv = MTB_ColorCorrect.rgb_to_hsv_torch(adjusted)

            # Применение коррекций:
            hsv[..., 0] = (hsv[..., 0] + hue) % 1.0  # Оттенок (циклический)
            hsv[..., 1] = hsv[..., 1] * saturation  # Насыщенность (масштабирование)
            hsv[..., 2] = hsv[..., 2] * value  # Яркость HSV (масштабирование)

            # Обратная конвертация в RGB
            adjusted = MTB_ColorCorrect.hsv_to_rgb_torch(hsv)

        # ОГРАНИЧЕНИЕ ЗНАЧЕНИЙ
        # При clamp=True значения ограничиваются [0,1] для стандартных изображений
        # При clamp=False сохраняются значения вне диапазона для HDR и спецэффектов
        if clamp:
            adjusted = torch.clamp(adjusted, 0.0, 1.0)

        # ПРИМЕНЕНИЕ МАСКИ
        # Маска определяет области применения эффекта:
        # Белый (>0.5) = применить эффект, Черный (≤0.5) = оставить оригинал
        if mask is not None:
            result = torch.where(mask > 0.5, adjusted, image)
        else:
            result = adjusted

        return (result,)


# Node class mappings for ComfyUI
NODE_CLASS_MAPPINGS = {
    "MTB_ColorCorrect": MTB_ColorCorrect
}

NODE_DISPLAY_NAME_MAPPINGS = {
    "MTB_ColorCorrect": "Цветокоррекция (Продвинутая)"  # Отображаемое имя в интерфейсе
}